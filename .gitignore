# Raw source archives (re-extract into content/ with extract.py + ocr.py)
drive-download-*/
*.docx
*.pdf

# Downloaded OCR model (re-download to re-run ocr.py)
tessdata/

# Temp / logs / cache
*.err
__pycache__/
content/img/
.DS_Store
.idea/
.vscode/

# Tooling — keep only the html/css/js site in the repo
# Python scripts (OCR / extraction / cleanup pipeline)
*.py
# Shell scripts
*.sh
# Claude Code skill / commands
.claude/

# Raw source material (1.3G) — mirror lives on server ~/Work/hoason
hoason/
